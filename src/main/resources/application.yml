spring:
  application:
    name: "config-server"
  profiles:
    active: native
    cloud:
  cloud:
    config:
      server:
        native:
          search-locations: "classpath:/config"

server:
  port: 8071
